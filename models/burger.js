var orm= require("../config/orm.js");


module.exports = function(){
    //uses the ORM commands to update/display the website 

    // one for getting all the information from the database

    // one for changing the status of a burger from not devoured to devoured

    // one for adding a burger to the database
}